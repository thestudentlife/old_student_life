<h1>Listing sections</h1>

<table>
  <tr style="text-align:left;">
    <th>Name</th>
    <th>Priority</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sections.each do |section| %>
  <tr>
    <td><%= section %></td>
    <td style="text-align: right;"><%= section.priority %></td>
    <td><%= link_to 'Edit', edit_workflow_section_path(section) %></td>
    <td><%= link_to 'Destroy', workflow_section_path(section), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section', new_workflow_section_path %>
