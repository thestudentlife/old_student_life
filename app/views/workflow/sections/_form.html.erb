<%= form_for([:workflow, @section]) do |f| %>
  <% if @section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@section.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @section.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div class="field">
	  <%= f.label :url, "URL" %>
	  <%= f.text_field :url %>
	</div>
	<div class="field">
		<%= f.label :priority %>
		<%= f.text_field :priority %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
