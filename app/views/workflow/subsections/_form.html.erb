<%= form_for([:workflow, @section, @subsection]) do |f| %>
  <% if @subsection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subsection.errors.count, "error") %> prohibited this section from being saved:</h2>

      <ul>
      <% @subsection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div class="field">
		<%= f.label :name %><br />
		<%= f.text_field :name %>
	</div>
	<div class="field">
		<%= f.label :priority %><br />
		<%= f.text_field :priority %>
	</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
