<%= form_for [:workflow, @review_slot] do |f| %>
  <% if @review_slot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_slot.errors.count, "error") %> prohibited this review_slot from being saved:</h2>

      <ul>
      <% @review_slot.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :requires_admin %>
    <%= f.check_box :requires_admin %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
