<% content_for :header do %>
  <h1>Add review of article</h1>
<% end %>

<%= form_for [:workflow, @article, @review], :url => workflow_article_reviews_path(@article) do |f| %>
  <% if @review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% @review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	  <%= f.label :review_slot_id, "Review slot" %>
	  <%= collection_select :workflow_review, :review_slot_id,
   	    current_user.open_review_slots_for_article(@article), :id, :to_s %>
	</div>
  <div class="field wide-field">
		<%= f.label :comment %> (optional)<br />
		<%= f.text_area :comment %>
   </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
