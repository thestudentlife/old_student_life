<h1>Listing images</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @images.each do |image| %>
  <tr>
    <td><%= link_to 'Show', workflow_image_path(image) %></td>
    <td><%= link_to 'Edit', edit_workflow_image_path(image) %></td>
    <td><%= link_to 'Destroy', workflow_image_path(image), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Image', new_workflow_image_path %>
