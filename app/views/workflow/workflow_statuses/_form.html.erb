<%= form_for [:workflow, @workflow_status], :url => url do |f| %>
  <% if @workflow_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workflow_status.errors.count, "error") %> prohibited this workflow_status from being saved:</h2>

      <ul>
      <% @workflow_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :requires_admin %>
    <%= f.check_box :requires_admin %>
  </div>
  <div class="field">
    <%= f.label :open_to_author %>
    <%= f.check_box :open_to_author %>
  </div>
  <div class="field">
    <%= f.label :publishable %>
    <%= f.check_box :publishable %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
