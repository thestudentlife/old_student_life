<%= form_for(@workflow_status) do |f| %>
  <% if @workflow_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workflow_status.errors.count, "error") %> prohibited this workflow_status from being saved:</h2>

      <ul>
      <% @workflow_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
